{% extends 'base.html.twig' %}

{% block title exception.message %}

{% block caption exception.statusText %}

{% block content %}
    <div class="text-center">
        <div class="error mx-auto" data-text="{{ exception.statusCode }}">{{ exception.statusCode }}</div>
        <p class="lead text-gray-800 mb-5">{{ exception.statusText }}</p>
        <p class="text-gray-500 mb-0">{{ exception.message }}</p>
        <a href="{{ path('app_index_index') }}">← Back to Dashboard</a>
    </div>
{% endblock %}
